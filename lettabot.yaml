server:
  mode: api
  apiKey: "placeholder"

agents:
  - name: "Ezra"
    id: "agent-3dc78a2f-bc9c-45c4-bd6f-9b405abe8f81"  # RAW ID only, no brackets or links!
    features:
      allowedTools: [web_search, conversation_search, fetch_webpage, bash, read, write, edit, glob, grep, task]
      permissionMode: "bypassPermissions"  # This is where the magic happens!
    conversations:
      mode: shared
      heartbeat: last-active
    channels:
      discord:
        enabled: true
        token: "placeholder"
        groupDebounceSec: 10
        groups:
          "1485788448672448592": { mode: mention-only }
          "1485743295396188382": { mode: mention-only }

providers:
  - id: google
    name: lc-google
    type: google_ai_studio
    apiKey: "placeholder"
